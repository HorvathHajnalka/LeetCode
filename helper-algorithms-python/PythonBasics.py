#True, False nagy betűvel
#min fgv min(len(str1), len(str2))
#listából string -> str1 = " " str1.join(lista))
#stringből lista ->  s = list(s)
#nincs olyan, hogy i++
#positive_infinity = float('inf'), negative_infinity = float('-inf')

"""Remove all items: clear()
Remove an item by index and get its value: pop()
Remove an item by value: remove()
Remove items by index or slice: del
    """
#The and operator returns true if both conditions are true, so the while loop will only run when it is True and True.

#from int to float -> float()
#hash map -> key exist -> if keyvalue in dictname, bejaras -> for kulcs in szotar

#append-> egy elemet ad hozzá, extend -> részlistával bővíti ki